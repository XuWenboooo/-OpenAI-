# 协议转换课题 · 完成度评测报告

> 评测日期：2026-09-01 · 评测基准：《协议转换课题_执行方案 v3》（v2 已被 v3 取代，以 v3 为准）
> 评测方式：通读全部源码 + 实跑测试与实验验证

## 0. 总体结论

**总体完成度：约 75%**

- 以「mock 离线演示 + 量化取舍文档」口径计：**约 90%**（M0–M7 骨架全部落地，实验 11 组全部符合预期且可复现）
- 以方案 v3 全量口径（含真实 API 验证、SSE、网关侧记忆注入管线）计：**约 75%**

一句话：项目的「骨架和灵魂」（IR 契约、三对 adapter、状态层、埋点、实验、量化文档）已经立住且质量较高；差的是「真实链路的最后一公里」——4 断点布局目前只存在于 mock，网关真实请求不会带 cache_control 断点，真实 API 对照实验还跑不了。

## 1. 实测验证结果（2026-09-01 实跑）

| 验证项 | 结果 |
|---|---|
| `tests/test_adapters.py` | ✅ 通过（注册表 + 三协议 roundtrip） |
| `tests/test_gateway.py` | ✅ 通过（三端点 e2e + 第二次命中缓存 cached_tokens=10 + 埋点汇总） |
| `src.experiments.run_experiments` | ✅ 11 组全部「✔符合」预期，结果与 `data/experiment_results.csv` 一致，可复现 |
| M6 面板启动日志 | ✅ `_panel_out.txt` 显示随机端口 + token URL 正常打印 |

## 2. 各维度评价

### 2.1 功能完整性 —— 70%

**已完成（对照 README M0–M7 与方案 3.4）：**
- M0 契约层：IR v0 三层结构（L0 内容块 / L1 规范请求 / L2 会话缓存上下文）完整，v3 关键常量齐备（4 断点上限、20-block 回看窗口、非单调模型阈值表）
- M1 可观测：方案 3.3 五项必暴露指标全部落库（命中率 / 注入 token / 重放 token / 丢弃参数 / 降级路径），OpenAI 折扣按代次（v3 修正已落实），对账口径正确（prompt_tokens - cached_tokens）
- M2 状态层：会话表 + TTL + previous_response_id 反查 + TRACK04 task-id 单键兜底（find_active_by_task）+ 重放起点规则（window/full）
- M3 适配层：三对 adapter 双向转换 + 丢弃参数/降级路径记录；max_tokens=0 预热语义保留
- M4 网关：三端点路由 + 限流（Semaphore=2）+ 预热单独路径 + 预热拒绝条件校验（v3 已核实的 5 条）+ mock/真实双模式
- M5 实验层：11 组对照（含 v3 重写的 G2 三档：512/1024/4096）+ 先串行预热再计量（v3 方法学前置）+ 盈亏平衡计算
- M6 面板：缓存前缀可视化 + 记忆勾选裁剪 + v3 安全骨架五项（127.0.0.1 / 随机端口 / Host 校验 / 一次性 token / 脱敏）
- M7 交付层：`docs/量化取舍文档.md` 质量高，结论、断点布局、有损字段清单、对照表齐全

**缺失/未完成：**
1. **真实上游不渲染 cache_control 断点（最关键缺口）**：`AnthropicUpstream.complete()` 构造请求时完全不带 `cache_control` 字段，IR 里的 `cache_control: list[int]` 断点下标没有任何下游消费者。4 断点布局（3 固定 + 1 滚动）目前只存在于 mock 模拟中——真实 API 实验跑不出方案要的断点效果
2. **网关侧没有记忆注入管线**：M6 面板裁剪的记忆写在 `session.meta["memories"]`，但网关 `handle()` 从不读取它注入 IR 请求；「记忆注入 + 幂等去重」只在实验脚本的 mock builder 里存在，线上链路是断的
3. **SSE 流式透传未实现**：README 声称「SSE 透传」，但 `stream=True` 解析后无任何处理，`BaseAdapter.stream_chunks_from_ir` 返回空列表且无人调用，网关永远返回非流式 JSON
4. 工具 ID 映射表（方案 9/3–9/6 产出物）无独立文档/模块（目前仅靠 IR 透传 id，OpenAI `call_xxx` ↔ Anthropic `toolu_xxx` 风格差异无映射层）
5. 能力矩阵 v0（可配置、注明数据日期）未见独立产物
6. 真实验证项全部未做（README 已自知）：D2 prev_id 冲突、20-block 真实边界、count_tokens RTT、4096 档大块复测

### 2.2 代码质量 —— 85%

**优点：**
- 模块边界清晰，注释与方案条款互相引用（每条关键设计都标注了方案出处），可维护性好
- SQLite 短生命周期连接 + WAL + 自动迁移补列，工程细节扎实
- mock 上游的缓存语义（阈值查表、回看窗口、预热写条目）准确对齐 v3 修正口径

**问题：**
1. `upstream.py` 的 `OpenAIUpstream.complete()` 有一段死代码：先用 `OpenAIChatAdapter.from_ir()` 构造 payload，随后立即被手工构造的 payload 覆盖（第 88–96 行白费）；且手工 payload 不带 tools、丢弃 tool_use 块
2. `AnthropicUpstream.complete()` 只取响应第一个 content block 的 text，真实链路 tool_use 块丢失——真实模式下工具调用往返会断
3. **盈亏平衡误判 bug**：`break_even_rows()` 中 G1/G2-4096/G4 三组 creation_cost=0、saving=0，`0 >= 0` 判定为「回本」——CSV 里这三组显示「回本」具有误导性（量化文档表格中正确地写成了「—」，代码与文档不一致）
4. `_estimate_injected_tokens` 用 `len(text)` 字符数充当 token 数；`_estimate_replay_tokens` 用「轮数 × 120」硬编码——作为占位可接受，但埋点数据的 ②③ 两项目前是示意值而非真实 token 口径
5. 网关 `handle()` 的异常一律转 502，mock 模式下也会吞掉 adapter 层的解析错误，排障不友好

### 2.3 文件组织 —— 90%

**优点：** 目录结构（ir / adapters / state / gateway / observability / experiments / webpanel）与方案模块划分一一对应；README 的模块分工表、快速开始、真实上游说明完整准确。

**问题：**
- 根目录残留调试文件 `_panel_out.txt` / `_panel_err.txt`，且 `_panel_out.txt` 里含面板一次性 token（虽是一次性的，仍建议清理并加 .gitignore）
- 无 `.gitignore`（`data/*.db`、`*-wal`、`*-shm`、`__pycache__` 都有入库风险）
- 微信导出的 PDF/docx 与项目文件混放根目录，建议移入 `docs/refs/`

### 2.4 测试覆盖 —— 60%

**已有：** adapter 三协议 roundtrip（含 tool_use/tool_result/prev_id 断言）+ 网关三端点 e2e（含缓存命中断言），全部实跑通过。

**缺口：**
- 无状态层测试（SessionStore 的 TTL 淘汰、prev_id 反查、重放窗口）
- 无可观测层测试（usage 解析的对账口径、summary 聚合）
- 无预热拒绝条件测试（`_validate_warmup` 的 5 条规则是 v3 重点，一行测试都没有）
- 无 thinking 块往返测试（方案 W2 任务「思考块往返」）
- 实验结论靠 print + 人工对照，未断言化（`expect_high` 字段已存在，很容易改成自动 assert）
- 无 pytest 配置（tests 是脚本式，pytest 直接收不了 `test_gateway.py` 里的 async 函数）

### 2.5 文档 —— 95%

`量化取舍文档.md` 是全项目最成熟的产物：结论先行、实验表格与代码实跑结果一致、v3 修正点全部落实。扣分项：`ir-schema.md` 第 5 节冻结签字三个复选框全未勾（IR 尚未正式冻结，05A/05B 并行开发的契约前提未闭环）。

## 3. 待办清单（按优先级）

> 更新于 2026-09-01 14:33。下方「已完成」为 2026-09-01 下午至晚间陆续落地的整改（详见第 5 节整改记录）；「剩余待办」为当前仍未闭环的事项。

### 已完成（本轮已落地，供评审回溯）

- ✅ P0-1 真实上游渲染 cache_control 4 断点布局 + `response_to_ir` 修复丢 tool_use 块
- ✅ P0-2 网关记忆注入管线（session meta → system 尾部 + 文本幂等去重）
- ✅ P0-3 `break_even_rows` 零成本误判修复
- ✅ P0-4 SSE 流式透传（网关 stream 分支 + upstream.stream + 三协议 stream_chunk）
- ✅ P1-5 `OpenAIUpstream` 死代码清理 + 真实模式 tools 转发
- ✅ P1-6 新增 `tests/test_core.py`（9 项），pytest 全量 **16 passed**
- ✅ P1-7 injected/replay token 估算标注「字符/示意口径」
- ✅ P2-10 `.gitignore` + `_panel_*.txt` 已手动删除 + 参考资料移入 `docs/refs/`
- ✅ P2-11 webpanel `_check_host` 取舍已加注释
- ✅ P2-12 重放起点→缓存前缀接线 `gateway._apply_replay`（按 prev_id 反查 + 重建历史前缀拼入 IR.messages）
- ✅ **P1-8 工具 ID 映射表 + 能力矩阵 v0**（方案 W1/W2 产出物，见 `docs/工具ID映射表与能力矩阵.md`）

### 剩余待办（未完成）

#### ⚠️ 真实 API 验证（需 API key，阻塞量化实验真实数据）
1. D2 `previous_response_id` 冲突：多轮 stateful 请求的 prev_id 状态键正确性（代码已用 task-id 单键兜底，待真实跑通确认）
2. 20-block 窗口真实边界：断点3（历史静态段末）的真实命中率 vs mock 估算
3. count_tokens RTT 成本：真实计量前缀 token 的成本评估（方案 3.5 点名）
4. 4096 档大块复测：超大 system 块真实缓存边界

#### 🔄 流程与治理（依赖外部确认，不阻塞现有功能）
5. `ir-schema.md` 三方签字冻结
6. TRACK 04 在参数层面签字确认 Session 边界三耦合点（TTL / 最大轮次 / 记忆上限；当前默认值可独立运行）
7. 向导师确认自仓库 PR 考核口径（v3 标 ⚠️ 风险项）
8. 提交 PR（代码侧已就绪并推送到 GitHub `master`，但尚未走 PR 评审流程）

## 4. 进度对照（方案 3.4 v3 重排表）

| 阶段 | 计划 | 实际状态 |
|---|---|---|
| 9/1–9/2 冻结契约 | IR v0 + 埋点 + 单轮链路 | ✅ 代码完成；⚠️ 签字未闭环 |
| 9/3–9/6 打通主链路 | adapter 多轮+工具、状态层、工具 ID 映射 | ✅ 主体完成；工具 ID 映射表 + 能力矩阵 v0 已交付 |
| 9/7–9/9 记忆注入与缓存 | 注入+幂等、4 断点布局、弹网页 | 🟡 mock 侧完成；真实链路断点渲染 + 网关注入管线缺失（P0-1/P0-2） |
| 9/10–9/12 量化实验 | 三组对照 + 20-block 复现 + 跨组 Review | 🟡 mock 实验完成且 11 组全符合；真实 API 实验未开始 |
| 9/13 交付整理 | PR + 量化文档 + 演练 | 🟡 量化文档已完成；PR 未提 |

## 5. 整改记录（2026-09-01 下午，本轮已落地）

| 编号 | 事项 | 状态 |
|---|---|---|
| P0-1 | 适配层补齐 `ir_to_payload`（含 cache_control 4 断点布局）/ `response_to_ir`，真实上游复用并修复丢 tool_use 块、死代码 | ✅ |
| P0-2 | 网关记忆注入管线（session meta → system 尾部 + 文本幂等去重） | ✅ |
| P0-3 | `break_even_rows` 零成本误判修复（输出「—（无可缓存内容）」） | ✅ |
| P0-4 | SSE 流式透传实现（网关 stream 分支 + upstream.stream + 三协议 stream_chunk） | ✅ |
| P1-5 | `OpenAIUpstream` 死代码清理，真实模式补 tools 转发 | ✅（并入 P0-1） |
| P1-6 | 新增 `tests/test_core.py`（7 项：状态层/预热/对账/断点/注入/SSE/实验断言），pytest 全量 14 passed | ✅ |
| P1-7 | injected/replay token 估算标注「字符/示意口径」 | ✅ |
| P2-10 | 新增 `.gitignore`；`_panel_*.txt` 已由用户手动删除；参考资料移入 `docs/refs/` | ✅ |
| P2-11 | webpanel `_check_host` 放行 localhost 的取舍已加注释说明 | ✅ |
| P2-12 | 重放起点→缓存前缀接线：新增 `gateway._apply_replay`（按 prev_id 反查、从存储历史轮次重建完整前缀拼入 IR.messages），`get_replay_history` 此前只存方法未接线现已接入网关；新增 2 项测试（16 passed） | ✅ |
| P1-8 | 工具 ID 映射表 + 能力矩阵 v0（W1/W2 产出物，`docs/工具ID映射表与能力矩阵.md`） | ✅ |

> 测试回归：`pytest tests/ -q` → **16 passed**（adapter 4 + core 9 + gateway 3）；M5 实验 11 组仍全部 ✔ 符合预期。
